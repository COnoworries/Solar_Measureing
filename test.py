# import pyudev
import yaml

# context = pyudev.Context()
# device_list = (device.device_node for device in context.list_devices(subsystem='block', DEVTYPE='partition'))
# print(device_list)

print(yaml_file["USB_FOLDER"]["STATUS"])